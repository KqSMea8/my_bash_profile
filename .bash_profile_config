
profilelist=(
~/.my_bash_profile/.bash_profile_directory
~/.my_bash_profile/.bash_profile_svn
~/.my_bash_profile/.bash_profile_git
~/.my_bash_profile/.bash_profile_rake
)

for file in ${profilelist[@]}
do
	if [ -f $file ]; then
	   source $file
	fi
done

alias resource='source ~/.bash_profile'
export PS1="\u[\W]\$ "
